apiVersion: v1
kind: Pod
metadata:
  name: hello-world-piggy-ms-pod
  namespace: jenkins-ns
  labels:
      app: hello-world-piggy
spec:
  containers:
    - name: hello-world-piggy-config
      image: __IMAGE_PLACEHOLDER__
      ports:
        - containerPort: 8888
      readinessProbe:
        httpGet:
          path: /actuator/health
          port: 8888
        initialDelaySeconds: 10  # Wait 30 seconds before first probe
        periodSeconds: 20       # Check every 10 seconds
        timeoutSeconds: 15        # Allow 5 seconds for a response
      livenessProbe:
        httpGet:
          path: /actuator/health
          port: 8888
        initialDelaySeconds: 10
        periodSeconds: 20
        timeoutSeconds: 15
  imagePullSecrets:
    - name: docker-reg-creds-secret  #credentials to pull images from docker registry