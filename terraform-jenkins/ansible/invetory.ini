[jenkins_controller]
${aws_instance.jenkins_controller.public_ip}

[jenkins_agents]
%{ for ip in aws_instance.jenkins_agents[*].public_ip ~}
${ip}
%{ endfor ~}

[all:vars]
ansible_user=admin 
ansible_ssh_private_key_file=${var.private_key_path}
ansible_host_key_checking=False
